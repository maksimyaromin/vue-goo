export default class Location {
    constructor({
        city,
        country,
        postcode,
        state,
        street
    }) {
        this.city = city;
        this.country = country;
        this.postcode = postcode;
        this.state = state;
        this.street = street;
    }
}
